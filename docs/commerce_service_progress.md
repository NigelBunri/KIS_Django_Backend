# Commerce Service Progress
- **Completed tasks**
  - Phase 0 discovery: architecture review, active/passive field audit, risk mapping, and documentation scaffolding.
  - Phase 1 scheduling correctness: feature-flagged buffer, coverage/travel, remote-region, and capacity validations with optional booking metadata plus logging.
  - Phase 2 commercial logic: quote/negotiation flows, package/addon pricing calculations, minimum charge enforcement, and tax adjustments with metadata persistence.
  - Phase 3 requirements/policy enforcement: requirements acknowledgements, service terms gating, refund window enforcement, and reschedule metadata/history with logging flags.
  - Payment receipts now capture each transaction via `ServiceBookingReceipt`, and the `/receipt/` endpoint returns deposit vs remaining snapshots so each payment produces its own HTML/PDF document.
  - Added `/receipt/regenerate/` plus the mobile “Generate new receipt” control so payers can refresh the latest PDF/HTML after final payment without needing to download stale documents.
- **In-progress tasks**
  - Phase 4 (broadcast & discovery): featured/tag ranking, additional metadata exposure, and feed filtering enhancements.
- **Blocked tasks**
  - Full automated regression verification is still blocked: the sandbox Postgres endpoint at 127.0.0.1:5432 refuses connections (`Operation not permitted`), and SQLite continues to choke on `commerce.0021_remove_shopservice_currency_shopservice_addons_and_more` because the JSON defaults include `[]` values that SQLite cannot parse. Manual Feature-flag toggling awaits an accessible PostgreSQL host.
- **Deferred tasks**
  - None yet; every future phase is tracked in the handoff doc.
- **Tests added**
  - Buffer conflict blocking (`SERVICE_ENFORCE_BUFFERS`).
  - Coverage/travel-radius enforcement (`SERVICE_ENFORCE_COVERAGE`, `SERVICE_ENFORCE_TRAVEL_RADIUS`).
  - Remote region gating (`SERVICE_ENFORCE_REMOTE_REGIONS`).
  - Participant/staff limits (`SERVICE_ENFORCE_GROUP_CAPACITY`).
  - Quote-required flow blocking payment (`SERVICE_ENABLE_QUOTES`).
  - Negotiation metadata + requested price tracking (`SERVICE_ENABLE_NEGOTIATION`).
  - Package pricing adjustments (`SERVICE_ENABLE_PACKAGE_PRICING`).
  - Add-on pricing adjustments (`SERVICE_ENABLE_ADDONS`).
  - Minimum charge enforcement (`SERVICE_ENFORCE_MINIMUM_CHARGE`).
  - Tax-inclusive adjustments and metadata (`SERVICE_HANDLE_TAX_INCLUSIVE`, `COMMERCE_DEFAULT_TAX_RATE_PCT`).
  - Requirements acknowledgements (`SERVICE_ENFORCE_REQUIREMENTS`).
  - Terms acceptance gating (`SERVICE_REQUIRE_TERMS_ACCEPTANCE`).
  - Refund policy window and reschedule metadata checks (`SERVICE_ENFORCE_REFUND_POLICY`, `SERVICE_ENFORCE_RESCHEDULE_POLICY`).
- **Tests executed**
  - `python3 manage.py test apps.commerce.tests.ServiceBookingAPITests` (fails because the sandbox Postgres endpoint at 127.0.0.1:5432 is unavailable: `OperationalError: connection is bad`).
  - `DATABASE_URL=sqlite:////tmp/testdb.sqlite3 python3 manage.py test apps.commerce.tests.ServiceBookingAPITests` (runs migrations but fails on `commerce.0021_remove_shopservice_currency_shopservice_addons_and_more` with `sqlite3.OperationalError: near "[]": syntax error`).
  - `DATABASE_URL=postgresql://kis_dev_user@localhost:5432/kis_test python3 manage.py migrate` (fails because the connection attempt to 127.0.0.1:5432 is blocked with `Operation not permitted`).
- **Migrations added**
  - `apps/commerce/migrations/0025_add_servicebooking_metadata.py` adds the `metadata` JSONField to `ServiceBooking`.
  - `apps/commerce/migrations/0035_servicebookingreceipt.py` creates `ServiceBookingReceipt` so each deposit or remaining payment produces its own receipt snapshot and URL.
- **Manual verification status**
  - Not run (database limitations prevent completing the suite; a PostgreSQL-compatible instance is required to finish the regression run).
